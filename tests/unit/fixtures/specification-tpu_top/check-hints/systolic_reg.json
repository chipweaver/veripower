[
  {
    "check_id": "CHK-SREG-01",
    "source_feature": "F-02",
    "implementation_detail": "out1 = in1 delayed 1 cycle via register delay10",
    "implementation_detail_verbatim": "`out1` = `in1` delayed **1** cycle (one register `delay10`); `delay10 <= in1`; `out1 = delay10`",
    "observable": "`out1` (`mac00_in`)",
    "reference_rule": "out1[t] == in1[t-1]",
    "latency": "1 cycle",
    "reset_behavior": "`delay10` cleared to 0 on async active-low reset (`negedge i_rstn`)"
  },
  {
    "check_id": "CHK-SREG-02",
    "source_feature": "F-02",
    "implementation_detail": "out2 = in2 delayed 2 cycles via cascaded delay20 then delay21",
    "implementation_detail_verbatim": "`out2` = `in2` delayed **2** cycles (two registers `delay20` → `delay21`); `delay20 <= in2`; `delay21 <= delay20`; `out2 = delay21`",
    "observable": "`out2` (`mac10_in`)",
    "reference_rule": "out2[t] == in2[t-2]",
    "latency": "2 cycle",
    "reset_behavior": "`delay20` and `delay21` cleared to 0 on async active-low reset (`negedge i_rstn`)"
  },
  {
    "check_id": "CHK-SREG-03",
    "source_feature": "F-02",
    "implementation_detail": "async active-low reset clears all three skew registers to 0",
    "implementation_detail_verbatim": "Async active-low reset clears all skew registers to 0; `delay10`/`delay20`/`delay21` <= 0 on `negedge i_rstn`",
    "observable": "`delay10`, `delay20`, `delay21`, `out1`, `out2`",
    "reference_rule": "on i_rstn==0: delay10==delay20==delay21==0 → out1==out2==0",
    "latency": "0 cycle (async)",
    "reset_behavior": "all skew registers (`delay10`, `delay20`, `delay21`) = 0"
  },
  {
    "check_id": "CHK-SREG-04",
    "source_feature": "F-02",
    "implementation_detail": "out1 leads out2 by 1 cycle for a same-cycle input pair (diagonal wavefront stagger)",
    "implementation_detail_verbatim": "`out1` = `in1` delayed **1** cycle (one register `delay10`); `out2` = `in2` delayed **2** cycles (two registers `delay20` → `delay21`)",
    "observable": "`out1` (`mac00_in`), `out2` (`mac10_in`)",
    "reference_rule": "for pair presented at t: out1 valid at t+1, out2 valid at t+2 (out1 leads out2 by 1)",
    "latency": "1 cycle (lead)",
    "reset_behavior": "both lines restart latency from reset-release edge after `delay10`/`delay20`/`delay21` cleared to 0"
  }
]
