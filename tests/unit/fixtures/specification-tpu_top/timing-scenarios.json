[
  {
    "id": "SC-001",
    "interface_mode": "apb + data_in + ctrl (full compute pass)",
    "stimulus": "Load `W=[[1,2],[3,4]]` via APB (`W00..W11 = 1,2,3,4`); stream 4 input pairs `in1=1,3,5,7` / `in2=2,4,6,8` with `in1_en`/`in2_en`; assert `start`",
    "expected": "`done` rises; four result words are read back over four APB reads",
    "timing_constraint": "`done` must assert (no hang); APB reads return result-FIFO words",
    "exceptions": "If `start` deasserts mid-pass, `counter` resets to 0 and `done` does not assert; writes/reads at FIFO boundaries ignored"
  },
  {
    "id": "SC-002",
    "interface_mode": "apb + data_in + ctrl (full compute pass)",
    "stimulus": "Load `W=[[1,2],[2,3]]`; stream 2 input pairs `in1=9,11` / `in2=10,12`; assert `start`",
    "expected": "`done` rises; four APB reads return results",
    "timing_constraint": "`done` must assert (no hang); APB reads return result-FIFO words",
    "exceptions": "Same as SC-001"
  },
  {
    "id": "SC-003",
    "interface_mode": "ctrl + apb (re-run on existing contents)",
    "stimulus": "Re-assert `start` with no new weights/inputs",
    "expected": "`done` rises again on the existing pipeline/FIFO contents; four APB reads",
    "timing_constraint": "`done` must assert (no hang); APB reads return result-FIFO words",
    "exceptions": "Reading beyond available result words exercises result-FIFO empty/wrap behavior"
  }
]
