{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "spec-review.schema.json",
  "title": "specification semantic review (gating reviewer output)",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "stage", "module", "reviewed_children", "verdict", "has_critical", "findings"],
  "properties": {
    "schema_version": {"const": 1},
    "stage": {"const": "specification"},
    "module": {"type": "string"},
    "reviewed_children": {"type": "array", "items": {"type": "string"}},
    "verdict": {"enum": ["ok", "concerns"]},
    "has_critical": {"type": "boolean"},
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["child", "lens", "severity", "location", "summary"],
        "properties": {
          "child": {"type": "string"},
          "lens": {"enum": ["faithfulness", "conformance", "soundness", "unavailable"]},
          "severity": {"enum": ["critical", "important", "minor"]},
          "location": {"type": "string"},
          "summary": {"type": "string"}
        }
      }
    }
  }
}
