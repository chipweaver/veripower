{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://veripower.local/skills/specification/clocks.schema.json",
  "title": "clocks.json — the module's clock definitions (authored, never derived)",
  "description": "The sole numeric + relationship source for constraints/<TOP>.{sdc,sgdc} and for the TB primary_clock. Exactly one entry is relationship=='primary'; JSON Schema cannot express that, so derive-constraints fails loud on it.",
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "object",
    "required": ["name", "period_ns", "relationship"],
    "additionalProperties": false,
    "properties": {
      "name": {
        "description": "For a non-generated clock this is also the top-level port name create_clock binds to.",
        "type": "string",
        "minLength": 1
      },
      "period_ns": {
        "description": "Period in ns. Reaches the TB scaffold's primary_clock.period_ns, where it is halved to build the clock generator.",
        "type": "number",
        "exclusiveMinimum": 0
      },
      "relationship": {
        "description": "async clocks drive set_clock_groups -asynchronous (SDC) and a distinct -domain (SGDC).",
        "enum": ["primary", "synchronous-related", "async"]
      },
      "generated": {
        "description": "true for a divider/PLL output with no top-level port: derive-constraints emits no create_clock for it and records a create_generated_clock-deferred-to-RTL note instead. Omitted means false.",
        "type": "boolean"
      },
      "role": {
        "description": "Free text for human and agent readers. No script parses it.",
        "type": "string"
      }
    }
  }
}
