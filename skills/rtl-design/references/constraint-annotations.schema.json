{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://veripower.local/skills/rtl-design/constraint-annotations.schema.json",
  "title": "constraint-annotations.json \u2014 per-child SGDC/SDC annotations (authored by the stage from the reaped child reports)",
  "description": "Implementation facts for SGDC/SDC authoring, grouped by the RTL portion that owns them. Use actual module, net and pin names. Keep annotations separate from compilation inputs so an annotation-only edit does not invalidate simulation. An empty category records that no such annotation is needed; finalize validates structure, while tool stages resolve the named objects.",
  "type": "object",
  "additionalProperties": {
    "type": "object",
    "required": [
      "sgdc",
      "sdc"
    ],
    "additionalProperties": false,
    "properties": {
      "sgdc": {
        "type": "object",
        "required": [
          "sync_cell",
          "reset_synchronizer",
          "set_case_analysis",
          "quasi_static"
        ],
        "additionalProperties": false,
        "properties": {
          "sync_cell": {
            "description": "Module names of custom synchronizers this child authored.",
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "reset_synchronizer": {
            "description": "The synchronized-reset NETS this child drives out of a reset synchronizer \u2014 not the synchronizer module's name. SGDC's two synchronizer commands take different kinds of name: sync_cell takes the module, reset_synchronizer takes the net, and giving it a module name is a Fatal that aborts rule checking for the whole run (measured on vL-2016.06). The net is named in the scope that instantiates the synchronizer, so it is that child's to declare, not the one that wrote the module.",
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "set_case_analysis": {
            "description": "Test-control ports held at a constant during analysis.",
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "port",
                "value"
              ],
              "additionalProperties": false,
              "properties": {
                "port": {
                  "type": "string",
                  "minLength": 1
                },
                "value": {
                  "type": "string",
                  "minLength": 1
                }
              }
            }
          },
          "quasi_static": {
            "description": "Signals that cross domains but change only at configuration time.",
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      },
      "sdc": {
        "type": "object",
        "required": [
          "create_generated_clock",
          "set_multicycle_path",
          "set_false_path"
        ],
        "additionalProperties": false,
        "properties": {
          "create_generated_clock": {
            "description": "Divider/PLL outputs this child generates: the module and the pin the clock leaves on. clocks.json marks the clock `generated` and defers the pin to here.",
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "module",
                "pin"
              ],
              "additionalProperties": false,
              "properties": {
                "module": {
                  "type": "string",
                  "minLength": 1
                },
                "pin": {
                  "type": "string",
                  "minLength": 1
                }
              }
            }
          },
          "set_multicycle_path": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "set_false_path": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      }
    }
  }
}
